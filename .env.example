# Waitlist API database connection
# Prefer POSTGRES_URL from Vercel Storage integration.
POSTGRES_URL=

# Optional fallback if POSTGRES_URL is not present.
DATABASE_URL=

# Public site URL for canonical links, Open Graph, and JSON-LD (no trailing slash).
# Production: https://www.ripplestudio.app
VITE_SITE_URL=
